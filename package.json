{
  "name": "cookie-dev",
  "private": true,
  "version": "0.0.1",
  "description": "cookie-dev",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build --prerender",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.0.1",
    "@stencil/sass": "^1.4.1"
  },
  "dependencies": {
    "ionicons": "^5.5.1"
  }
}
